/**
 * Represents a {{name}} entity.
 * 
 * This interface defines the structure of an {{nameCamel}} object in the domain layer.
 * It includes basic {{nameCamel}} information such as `id`.
 * 
 * @interface {{name}}Entity
 */
export interface {{name}}Entity {
  /**
   * The unique identifier of the {{nameCamel}}.
   * @type {string}
  */
  id: string
}